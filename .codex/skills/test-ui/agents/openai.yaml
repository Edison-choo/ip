interface:
  display_name: "UI Test Runner"
  short_description: "Run command-and-output UI test cases"
  default_prompt: "Use $test-ui to run the cases in test/ui-test-plan.md, compare exact outputs, and record the console session."
